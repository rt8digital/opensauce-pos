# Database Configuration

# PostgreSQL connection string (required for database storage)
# Example: postgresql://user:password@host:5432/database
# For Neon: postgres://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=

# Environment mode
NODE_ENV=development

# Use in-memory storage instead of database (set to 'true' to use memory storage)
# Useful for testing or when database is not available
USE_MEMORY_STORAGE=false
